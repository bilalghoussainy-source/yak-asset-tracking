{
  "name": "yak-asset-tracking",
  "version": "1.0.0",
  "private": true,
  "description": "Yak Asset Tracking — multi-tenant via URL path, Supabase backend",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && npx --yes serve dist -p 3000"
  },
  "engines": {
    "node": ">=18"
  }
}
